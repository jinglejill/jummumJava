<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/colorBg"/>
    <padding
        android:left="@dimen/padding"
        android:right="@dimen/padding"
        android:top="@dimen/padding"
        android:bottom="@dimen/padding"></padding>
    <corners
        android:topLeftRadius="@dimen/padding"
        android:topRightRadius="@dimen/padding"/>
    <stroke android:width="1dp" android:color="@color/bgColorMain"></stroke>
</shape>